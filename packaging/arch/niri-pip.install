post_install() {
  cat <<'MSG'

niri-pip installed.
For each Niri user, run inside the Niri session:
  niripip-integrate
  systemctl --user enable --now niripip.service
  niripip doctor

Open the controller with:
  niripip menu

MSG
}

post_upgrade() {
  post_install
}
